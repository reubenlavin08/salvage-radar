# Core deps for the agent-based pipeline (no API key needed).
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
pydantic>=2.6.0
python-dotenv>=1.0.0
tenacity>=8.2.0

# Embeddings — pick ONE.
# Local (default): no account needed, ~90 MB model on first run, ~2 GB
# torch dep.
sentence-transformers>=2.7.0

# OR Voyage AI (Anthropic's recommended). Lightweight install, but
# requires VOYAGE_API_KEY in env. Uncomment to use.
# voyageai>=0.2.0

# Only needed for the legacy API path (triage.py / extractor.py /
# valuator.py / pipeline.py). The /appraise slash command does NOT
# require this.
# anthropic>=0.40.0
